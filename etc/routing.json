{
  "routes": [{
      "method": "post",
      "pattern": "/v1/auth/:method",
      "controller": "Api\\AuthenticationControllerGet",
      "rules": [],
      "defaults": [{
        "_format": "json"
      }]
    },
    {
      "method": "get",
      "pattern": "/v1/user/:method",
      "controller": "Api\\AuthenticationControllerGet",
      "rules": [],
      "defaults": [{
        "_format": "json"
      }]
    }
  ]
}